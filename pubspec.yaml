name: anysql
description: A Firebase-style keyword API, setup generator, HTTP backend client, and driver layer for SQL and NoSQL databases.
version: 0.3.0
repository: https://github.com/Azrul16/anysql
issue_tracker: https://github.com/Azrul16/anysql/issues
topics:
  - database
  - sql
  - nosql
  - cli
  - configuration

platforms:
  android:
  ios:
  linux:
  macos:
  windows:

environment:
  sdk: ^3.10.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0

executables:
  anysql:

dependencies:
  http: ^1.6.0
  mongo_dart: ^0.10.8
  mysql_client: ^0.0.27
  postgres: ^3.5.9
  sqlite3: ^3.3.1
